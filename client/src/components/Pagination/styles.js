import { makeStyles }  from '@material-ui/core/styles';

export default makeStyles(() => ({
    // A call back function that instantly returns an object. 
    ul: {
        justifyContent: 'space-around'
    }
}))