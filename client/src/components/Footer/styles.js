import { makeStyles } from '@material-ui/core/styles';

export default makeStyles (() => ({
    footer: {
        borderRadius: 50,
        alignItems: 'center',
        marginTop: '2rem',
        backgroundColor: 'transparent'
    }
}));