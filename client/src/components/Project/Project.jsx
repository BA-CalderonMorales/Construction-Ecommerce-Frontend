import React from 'react'
import Form from '../Form/Form';

const Project = ({ currentId, setCurrentId }) => {
    

    return (
        <Form currentId={currentId} setCurrentId={setCurrentId} />
    )
}

export default Project;
